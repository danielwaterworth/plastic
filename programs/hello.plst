define foo() -> bool do
    v := true;
    do
        a := 100;
        sys print_num(a);
    while (v)
    status := false;
    return status;
end

define main() -> bool do
    foo();
    v := false;
    return v;
end
