define main()
initial:
    a := 0;
    b := 1;
    c := 4;
    goto body;

body:
    i := phi (initial, a), (body, i_next);
    i_next := i + b;
    c := i_next < c;
    sys print_num(i);
    condition c body exit;

exit:
    v := 0;
    return v;
end
