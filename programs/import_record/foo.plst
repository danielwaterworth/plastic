record Foo
    attr foo : UInt;
    attr bar : UInt;

    constructor new(a : UInt)
        @foo := a;
        @bar := a;
    end

    define plah(a : UInt) -> Bool do
        return a < @bar;
    end
end
