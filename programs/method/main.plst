service Test()
    constructor new()
    end

    implements EntryPoint
        define main(args : List(ByteString)) -> Bool do
            sys print_string(1.to_string());
            return false;
        end
    end
end

entry
    return Test().new();
end
